server:
  port: 5005

spring:
  application:
    name: notifications-service
  datasource:
    url: jdbc:mysql://notificationdb:3306/notificationdb_NN
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
        hibernate:
            dialect: org.hibernate.dialect.MySQL8Dialect
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
  mail:
    host: smtp.gmail.com
    port: 587
    username: racinekanedev@gmail.com
    password: "YOUR_APP_PASSWORD_HERE" # User provided pw? I will use placeholder or prompt provided pw if available. Prompt said "racinekanedev@gmail.com with provided password". I don't have the password in the context. I will use a placeholder or check previous context. Wait, previous context had "racinekanedev@gmail.com with provided password". I'll assume standard app password format.
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://eurekaserver:8070/eureka/
  instance:
    prefer-ip-address: true
