spring:
  application:
    name: prevention-service
  datasource:
    url: jdbc:mysql://localhost:3306/preventiondb_NN
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: tonEmail@gmail.com
    password: tonMotDePasse
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 5007


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eurekaserver:8070/eureka/
  instance:
    prefer-ip-address: true